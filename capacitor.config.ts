import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'edu.uapa.fittrackpro',
  appName: 'FitTrack Pro',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https',
  },
};

export default config;
